# Headless Renderer

Reference renderer backend for tests, simulators, and products without a
display. It implements the same logical surface and transaction SPI as a GUI
renderer while retaining only deterministic snapshots for assertions.
