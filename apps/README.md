# Reference Applications

Standard PXA implementations of replaceable system roles and conformance Apps
belong here. They are ordinary signed PXA Packages and must use the same public
protocol as external Apps.

The current product examples remain in the parent firmware's `apps/pxa` tree
until their assets and product callbacks are separated. The package tool accepts
an external source tree through `PXA_APP_SOURCE_ROOT`.
