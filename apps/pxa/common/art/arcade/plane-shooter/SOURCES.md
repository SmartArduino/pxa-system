# Gameplay Image Sources

Plane, enemy, Boss, and pickup sprites are original project artwork rendered
from the editable SVG sources in this directory. They are drawn at their native
1:1 size with transparent margins. The player's engine exhaust is rendered from
game particles. Run `render_assets.sh` to regenerate the runtime PNG files in
`apps/pxa/arcade/assets/plane-shooter/`.
