# Gameplay Image Sources

`paddle.png` and `ball.png` are original project artwork rendered from the SVG
sources in this directory at their native in-game sizes.

`background.png` is original project artwork rendered from `background.svg`.
The generated runtime PNG files are stored in `apps/pxa/arcade/assets/brick-breaker/`.
