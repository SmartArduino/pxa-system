# Gameplay Image Sources

`player.png` and `background.png` are original project artwork rendered from
the SVG sources in this directory at their native in-game sizes. The generated
runtime PNG files are stored in `apps/pxa/arcade/assets/jumping/`.
