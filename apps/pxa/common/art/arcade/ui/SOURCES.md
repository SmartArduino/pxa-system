# UI Image Sources

All runtime PNG files are original project artwork rendered from the SVG
sources in this directory. They are stored in `apps/pxa/arcade/assets/ui/`,
carry panel and button material only, and leave text and state as dynamic
canvas content.
